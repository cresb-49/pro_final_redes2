acl localnet src 0.0.0.1-0.255.255.255  # RFC 1122 "this" network (LAN)
acl localnet src 10.0.0.0/8             # RFC 1918 local private network (LAN)
acl localnet src 100.64.0.0/10          # RFC 6598 shared address space (CGN)
acl localnet src 169.254.0.0/16         # RFC 3927 link-local (directly plugged) machines
acl localnet src 172.16.0.0/12          # RFC 1918 local private network (LAN)
#acl localnet src 192.168.0.0/16        # RFC 1918 local private network (LAN)
acl localnet src fc00::/7               # RFC 4193 local private network range
acl localnet src fe80::/10              # RFC 4291 link-local (directly plugged) machines

# SSL
acl intermediate_fetching transaction_initiator certificate-fetching
http_access allow intermediate_fetching

http_access allow localnet

acl ip_admin src 192.168.25.3
acl ip_externo src 192.168.25.4

acl CONNECT method CONNECT
acl SSL_ports port 443

acl admin_sites dstdomain .fast.com .google.com .duckduckgo.com
acl externo_sites dstdomain .edu4java.com

http_access allow ip_admin admin_sites
http_access allow ip_externo externo_sites

#Si esta linea esta comentada se habilitan todos los sitios
#http_access allow ip_admin SSL_ports
http_access deny all

# Puertos
http_port 3128 intercept
https_port 3129 intercept ssl-bump generate-host-certificates=on dynamic_cert_mem_cache_size=20MB cert=/etc/squid/ssl_cert/bump.crt key=/etc/squid/ssl_cert/bump.key

http_port 3128

sslcrtd_program /usr/lib/squid/security_file_certgen -s /var/lib/ssl_db -M 20MB
sslproxy_cert_error allow all
ssl_bump peek step1
ssl_bump bump all
ssl_bump splice all


via off
